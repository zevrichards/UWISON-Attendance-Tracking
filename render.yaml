services:
  - type: web
    name: uwison-attendance-tracking
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: ADMIN_PASSWORD
        sync: false          # Set this manually in the Render dashboard
      - key: DATABASE_URL
        fromDatabase:
          name: uwison-attendance-db
          property: connectionString
      - key: DEDUP_SECONDS
        value: 30

databases:
  - name: uwison-attendance-db
    plan: free
